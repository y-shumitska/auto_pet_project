#############################
# Environment example (local)
#############################

# UI login for setup tests (tests/setup/auth.setup.spec.ts)
# Replace these with NON-production test credentials in your real .env
LOGIN=your_login_here
PASSWORD=your_password_here

# CI flag – usually set automatically by CI
# Leave as-is locally; your CI will typically export CI=true
CI=false
